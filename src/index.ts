import Runner from './runner';
import Maker from './maker';
import runmake from './runmake';

export { Runner, Maker, runmake };
export const classes = { Runner, Maker };
export default { Runner, Maker, runmake, classes };
